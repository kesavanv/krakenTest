require(['config'], function (config) {
    "use strict";

    require(["router"],
        function (Router) {

            /* Create instance of the App */
            var app = {
                initialize : function () {

                }
            };

            /* Initialize the instance */
            app.initialize();
        });
});
